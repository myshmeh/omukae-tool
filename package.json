{
  "name": "omukae-tool",
  "version": "1.0.0",
  "description": "a tool for the Omukae culture among the illustration community on Twitter",
  "main": "main.js",
  "scripts": {
    "mockup": "node test/twittermock/mockup.js",
    "start": "node main.js | ./node_modules/.bin/bunyan",
    "dev": "export NODE_ENV=test && npm start",
    "prod": "export NODE_ENV=production && npm start",
    "format": "npx prettier --write src",
    "db-cleanup": "sqlite3 database/omukae-tool.db '.read database/drop-tables.sql'",
    "db-setup": "sqlite3 database/omukae-tool.db '.read database/create-tables.sql'",
    "test-db-cleanup": "sqlite3 database/$npm_config_dbname '.read database/drop-tables.sql'",
    "test-db-setup": "sqlite3 database/$npm_config_dbname '.read database/create-tables.sql'",
    "test-db-population": "sqlite3 database/$npm_config_dbname '.read database/populate-test.sql'",
    "test": "jest --verbose"
  },
  "author": "myshmeh",
  "license": "ISC",
  "dependencies": {
    "bunyan": "^1.8.15",
    "dotenv": "^8.2.0",
    "puppeteer": "^9.0.0",
    "sqlite3": "^5.0.2"
  },
  "devDependencies": {
    "jest": "^26.6.3",
    "prettier": "2.2.1"
  }
}
